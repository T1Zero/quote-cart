/**
 * Remix is configured via Vite (vite.config.ts) on this template.
 * This file is kept for tooling that still looks for it.
 *
 * @type {import('@remix-run/dev').AppConfig}
 */
export default {
  ignoredRouteFiles: ["**/.*"],
};
